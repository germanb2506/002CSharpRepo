namespace Businnes.Sintaxis
{
    internal class Bucles
    {
        private void MiMetodo()
        {
            // For usado cuando se conoce de antemano el numero de itearciones
            for (int i = 0; i < 10; i++) { }

            bool condicion = true;
            //While
            while (condicion)
            {
                // Codigo a ejecutar mientras se cumpla
            }
            do
            {
                //Ejecuta inicialmente el codigo y luego verifica si se cumple y si se cumple hace una segunda iteracion de lo contrario sigue la logica
            } while (condicion);
        }
    }
}
